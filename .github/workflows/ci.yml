## ==================================================================
## CI/CD - Nutra Frontend (vai no repo: nutra-frontend)
## ==================================================================
## Copie este arquivo para: nutra-frontend/.github/workflows/ci.yml
##
## ⚠️ ATENÇÃO: O frontend usa variáveis NEXT_PUBLIC_* em BUILD TIME.
## O build final contém as URLs "hardcoded" no JavaScript.
## As variáveis OIDC_*, NEXTAUTH_*, REDIS_* são runtime (server-side)
## e são injetadas via docker-compose environment no deploy.
## ==================================================================

name: CI/CD - Nutra Frontend

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/nutra-frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_NUTRA_API_URL=${{ vars.APP_BASE_URL }}/nutra-api/api
            NEXT_PUBLIC_API_URL=${{ vars.APP_BASE_URL }}/api

      - name: Trigger deploy
        if: github.event_name == 'push'
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.DEPLOY_REPO_TOKEN }}
          repository: ${{ github.repository_owner }}/deploy
          event-type: deploy-nutra-frontend
          client-payload: '{"sha": "${{ github.sha }}", "image": "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"}'
